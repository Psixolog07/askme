<div class="container">
  <h2 class='title-lg mb'>Вопрос <%= @question.id %></h2>
  <p class="mt-sm"><%= link_to "Все вопросы #{show_nickname(@question.user)}", user_path(@question.user.nickname) %></p>
</div>

<div class="mt-sm container">
  <%= render @question %>
</div>
